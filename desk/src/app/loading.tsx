export default function Loading() {
  return (
    <div className="mx-auto flex w-full max-w-lg flex-col items-center gap-4 py-20 text-center">
      <p className="text-sm font-semibold tracking-[0.2em] text-mx-accent uppercase">MonEx</p>
      <p className="text-sm text-mx-muted">Loading Trade Assistant…</p>
    </div>
  );
}
